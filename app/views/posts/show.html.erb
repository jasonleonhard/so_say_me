<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>


<div id="comments">
  <h2>Comments</h2>
  <!-- < % Expands to render partial: 'comments/comments', Collection: @post.comments %>   -->

  <!-- form -->
  <%= render 'comments/new', post: @post %>

  <!-- comments -->
  <%= render @post.comments %>
</div>